name: Production - Sync artifacts

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
    
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

